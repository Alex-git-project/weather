import * as React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import DetailsCity from "../screens";

export default function DetailsCityTab({navigation}) {
    return (
        <DetailsCity navigation={navigation} />
    )
}
